services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      OLLAMA_HOST: http://host.docker.internal:11434
      CHROMA_PATH: /app/chroma
    volumes:
      - ./chroma:/app/chroma
    extra_hosts:
      - "host.docker.internal:host-gateway"
